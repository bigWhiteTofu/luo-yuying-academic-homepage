{
  "name": "luo-yuying-academic-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute luo-yuying-academic-site --local --file=schema.sql",
    "db:remote": "wrangler d1 execute luo-yuying-academic-site --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
